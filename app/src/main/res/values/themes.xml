<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TrackingRunningApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
  
    <style name="Theme.TrackingRunningApp" parent="Base.Theme.TrackingRunningApp" />

    <style name="DefaultButton" parent="Theme.TrackingRunningApp">
        <item name="colorPrimary">#EEFF01</item>
    </style>

    <style name="MediumButton" parent="Theme.TrackingRunningApp">
        <item name="colorPrimary">@color/primary_gray</item>
    </style>

    <style name="TrackingButton" parent="Theme.TrackingRunningApp">
        <item name="colorPrimary">@color/black</item>
    </style>

    <style name="ThemeMaterialCalendar" parent="ThemeOverlay.Material3.MaterialCalendar">
        <item name="android:windowFullscreen">false</item>
    </style>

    <style name="CardView" parent="Widget.MaterialComponents.CardView">
        <item name="strokeColor">@color/main_yellow</item>
        <item name="strokeWidth">2dp</item>
    </style>
</resources>